export const ROUTES = {
	home: '/',
	branches: 'losungen',
	branchesCard: 'losungen/:id',
	favorites: 'favorites',
	all: '*',
};

export const {home, branches, branchesCard, favorites, all} = ROUTES;
